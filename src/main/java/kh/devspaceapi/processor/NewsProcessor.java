package kh.devspaceapi.processor;

public class NewsProcessor {
//    public void process(NewsResponse data) {
        // 1. data 를 insert 용 dto(NewsRequestDto or NewsInsertionDto) 로 변환
        // 변환하면서 데이터 정제

        // 2. dto -> entity 로 변환

        // 3. insert
        // newsPostRepository(entity)
//    }
}
